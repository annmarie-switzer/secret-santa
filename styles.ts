import { StyleSheet } from 'react-native';

export const vars = {
    textColor: 'orange',
    formFieldBorderColor: 'green',

};

export const globalStyles = StyleSheet.create({
    view: {
        flex: 1
    },
});
